{
  "name": "agentforge",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -b --pretty",
    "lint": "echo 'Configure ESLint/Next lint in CI pipeline'",
    "test": "echo 'Add unit/integration tests per service'"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
